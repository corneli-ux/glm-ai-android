# Example keystore.properties — DO NOT COMMIT your real one.
# Copy this file to `keystore.properties` and fill in your signing info,
# OR set GitHub Actions secrets (see README.md → Release signing).

storeFile=release.keystore
storePassword=CHANGE_ME
keyAlias=glm-ai
keyPassword=CHANGE_ME
